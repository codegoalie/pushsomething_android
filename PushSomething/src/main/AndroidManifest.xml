<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chrismar035.pushsomething"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <permission
        android:name="com.chrismar035.pushsomething.permissions.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.chrismar035.pushsomething.permissions.C2D_MESSAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.chrismar035.pushsomething.GcmBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />

                <category android:name="com.chrismar035.pushsomething" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.chrismar035.pushsomething.SignInActivity"
            android:label="@string/app_name"
            android:excludeFromRecents="true" >
        </activity>

        <service
            android:name="com.chrismar035.pushsomething.AcknowledgeService"
            android:enabled="true"
            android:exported="true" >
        </service>
        <meta-data android:name="api_root" android:value="http://pushsomething.com/api/v1/" />
        <meta-data android:name="com.crashlytics.ApiKey" android:value="0453ebaad38fa78337753e7852c7c3b1df9e42f4"/>
    </application>

</manifest>
